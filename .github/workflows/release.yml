name: CICD

# Triggers when pushed to main branch
on:
  push:
    branches:
      - main
    tags:
      - '!*'
permissions:
  id-token: write
  contents: read
  actions: read

jobs:
  release:
    name: release-npm-package
    runs-on: ubuntu-latest
    steps:
      - name: checkout
        uses: actions/checkout@v2
      - name: setup-node
        uses: actions/setup-node@v2
        with:
          node-version: 14.x
          registry-url: 'https://registry.npmjs.org'
      # build node modules from rust code
      - name: build by rust code
        run: wasm-pack build

      # Get current package version
      - name: current-package-version
        run: cd pkg && node -p -e '`CURRENT_PACKAGE_VERSION=${require("./package.json").version}`' >> $GITHUB_ENV

      - name: module install
        run: cd pkg && npm install

      - name: package build
        run: cd pkg && npm run build

      # Publish to npm
      - name: publish-to-npm
        run: cd pkg && npm publish --access=public --tag randomforest
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_ACCESS_TOKEN }}
